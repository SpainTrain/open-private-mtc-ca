# @generated by scripts/api-gen.sh — DO NOT EDIT.
# Regenerate with `make api-gen` after editing api/admin.openapi.yaml (spec §17.5).
[package]
publish = false
name = "mtc-admin-api-client"
version = "0.1.0"
authors = ["OpenAPI Generator team and contributors"]
description = "Administrative HTTP+JSON API for the Merkle Tree Certificate CA (spec §17.2). Consumed by `mtcctl` (via the generated Rust client) and the admin UI. Seed surface: health endpoints (§20.5) and a minimal service status endpoint (§17.3). "
license = "Apache-2.0"
edition = "2021"

[dependencies]
serde = { version = "^1.0", features = ["derive"] }
serde_json = "^1.0"
serde_repr = "^0.1"
url = "^2.5"
chrono = { version = "^0.4",  features = ["serde"] }
reqwest = { version = "^0.13", default-features = false, features = ["json", "multipart", "query", "form"] }

[features]
default = ["native-tls"]
native-tls = ["reqwest/native-tls"]
rustls = ["reqwest/rustls"]
# Clippy exemption for generated code (see header). Allowed lints emit no
# diagnostics, so `cargo clippy -- -D warnings` stays green without editing
# generated sources.
[lints.clippy]
all = { level = "allow", priority = -1 }
pedantic = { level = "allow", priority = -1 }
nursery = { level = "allow", priority = -1 }
