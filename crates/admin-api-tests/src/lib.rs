//! Contract tests for the generated admin API crates (spec §17.2).
//!
//! This library is intentionally empty: the tests live in `tests/`. They are
//! kept in their own crate so they survive `make api-gen`, which rewrites the
//! generated `admin-api-server` and `admin-api-client` crates wholesale.
