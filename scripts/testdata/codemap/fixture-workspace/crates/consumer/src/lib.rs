//! Fixture crate for scripts/codemap-smoke-test.sh — depends on
//! `fixture-base` so the generator's reverse-dependency (USED BY) edge has
//! something real to compute.

pub use fixture_base::Widget;
