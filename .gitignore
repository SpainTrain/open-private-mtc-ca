
# Rust
/target/
**/target/
*.rs.bk

# Node / TypeScript (infra/ CDK app, generated TS clients)
node_modules/
*.tsbuildinfo

# AWS CDK synth output
cdk.out/
**/cdk.out/

# LocalStack / dev-environment volumes and state
.localstack/
volume/
**/localstack-volume/
.softhsm/

# Beads / Dolt files (added by bd init)
.dolt/
*.db
.beads-credential-key
.beads/proxieddb/

# Task-scoped working set (spec §23.4) — working copies are never committed.
# The checked-in template lives at docs/templates/WORKING_SET.md.
/WORKING_SET.md

# Agent worktrees (transient; created by the Agent tool or `git worktree add`).
.claude/worktrees/
